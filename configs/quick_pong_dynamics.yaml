batch_size_per_gpu: 500
gradient_accumulation_steps: 1
n_updates: 300000
learning_rate: 0.01
log_interval: 2000

use_actions: true

embed_dim: 32
num_heads: 8
hidden_dim: 128
num_blocks: 8

video_tokenizer_path:
latent_actions_path:

checkpoint:
