# Training — video tokenizer for surgical frames
batch_size_per_gpu: 64
gradient_accumulation_steps: 2
n_updates: 60000
learning_rate: 0.001
log_interval: 2000

embed_dim: 48
num_heads: 8
hidden_dim: 192
num_blocks: 6

checkpoint:
